let firstDiv = document.querySelector('[title*="box"]');

//firstDiv.innerHTML = "I changed it!"
//attributes * means contains
const boxes = document.querySelectorAll('[title*="box"]');
// *= contains

//console.log(boxes);
//attributes in square brackets
const box = document.querySelector('[title*="box"]');
console.log(box);